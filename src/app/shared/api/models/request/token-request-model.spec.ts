import { TokenRequestModel } from './token-request-model';

describe('TokenRequestModel', () => {
  it('should create an instance', () => {
    expect(new TokenRequestModel()).toBeTruthy();
  });
});
