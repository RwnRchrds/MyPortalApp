import { BulletinSearchOptions } from './bulletin-search-options';

describe('BulletinSearchOptions', () => {
  it('should create an instance', () => {
    expect(new BulletinSearchOptions()).toBeTruthy();
  });
});
