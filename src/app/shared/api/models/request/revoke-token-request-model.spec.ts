import { RevokeTokenRequestModel } from './revoke-token-request-model';

describe('RevokeTokenRequestModel', () => {
  it('should create an instance', () => {
    expect(new RevokeTokenRequestModel()).toBeTruthy();
  });
});
